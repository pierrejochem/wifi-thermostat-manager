#include <tunables/global>

profile wifi_thermostat_manager flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/python>

  # s6-overlay v3 init system (container entrypoint = /init)
  /init ix,
  /package/** ix,
  /command/** ix,
  /etc/s6-overlay/** r,
  /run/** rwix,

  # Startup
  /run.sh ix,
  /usr/bin/python3 ix,
  /bin/** ix,
  /usr/bin/** ix,

  # Persistent storage
  /data/** rw,

  # Application
  /app/** r,

  # bashio
  /usr/lib/bashio/** ix,
}
