# The image ships the pre-built bundle (app/static/dist) and never builds the
# frontend, so the Node source is not needed in the build context.
frontend
.git
.github
.superpowers
docs/superpowers
dev/.venv
dev/data
**/__pycache__
*.pyc
