Flask==3.1.3
paho-mqtt==2.1.0
tinytuya==1.20.0
requests==2.34.2
# Reused to import devices straight from Home Assistant's Tuya integration.
# Its paho-mqtt dep is unpinned and it still builds clients on the callback v1
# API, which 2.x only deprecates (DeprecationWarning) rather than removes — so
# our 2.1.0 pin above satisfies it. If a future paho drops v1 outright, this
# has to stay behind until the SDK migrates.
tuya-device-sharing-sdk==0.2.14
