Flask==3.1.3
# Held at 1.x on purpose: the app uses the legacy callback API, which 2.x drops.
paho-mqtt==1.6.1
tinytuya==1.20.0
requests==2.34.2
# Reused to import devices straight from Home Assistant's Tuya integration.
# Unpinned paho-mqtt dep is satisfied by our 1.6.1 pin above (legacy callback API).
tuya-device-sharing-sdk==0.2.14
